/** Tamanhos de ícone unificados (MaterialCommunityIcons) */
export const iconSize = {
  xs: 16,
  sm: 18,
  md: 20,
  lg: 22,
  xl: 24,
} as const;

/** Chevron de listas e CTAs — sempre o mesmo tamanho */
export const chevronSize = iconSize.md;
